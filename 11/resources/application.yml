spring:
  datasource:
    url: jdbc:h2:./todos.h2;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  liquibase:
    change-log: classpath:changelogs/root-changelog.yml
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
